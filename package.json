{
    "name":  "fabric-supernet",
    "version":  "1.0.0",
    "private":  true,
    "scripts":  {
                    "smoke":  "node scripts/remote-compile.js --in examples/hello.fab --out out/hello.remote.ir.json --target sol",
                    "mint:runtime":  "powershell -NoProfile -ExecutionPolicy Bypass -File ./runtime/mint-runtime.ps1"
                },
    "devDependencies":  {
                            "@grpc/grpc-js":  "^1.10.8",
                            "@grpc/proto-loader":  "^0.8.0"
                        }
}
