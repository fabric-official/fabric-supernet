# Mobile Core — iOS integration

This directory is the iOS entry point that calls into the TypeScript SDK outputs in `dist/`.
Add Xcode build steps or a bridge to load the compiled outputs from `dist/`.