# ---------- E2E HARNESS ENV (copy to .env and edit) ----------
# Compose profile: 'real' to run your real backend; 'mock' to run only the dashboard and mocks
PROFILE=real

# Docker compose file (leave default unless you split)
COMPOSE_FILE=docker-compose.yml

# Contexts and Dockerfiles (edit to match your repo layout)
DASHBOARD_CONTEXT=../fabric-main
DASHBOARD_DOCKERFILE=Dockerfile

API_CONTEXT=../api
API_DOCKERFILE=Dockerfile

# Service ports exposed to host
DASHBOARD_PORT=5173
API_PORT=8080
REDIS_PORT=6379
POSTGRES_PORT=5432

# Health endpoints (relative to service; e.g., /health or /)
DASHBOARD_HEALTH=/
API_HEALTH=/api/health

# Base URLs used by Playwright tests
BASE_URL=http://localhost:${DASHBOARD_PORT}
API_BASE=http://localhost:${API_PORT}

# Test runner routes (comma-separated for dead-click sweep)
ROUTES=/,/pair,/wifi,/devices,/policies,/registry,/treasury,/dao,/telemetry,/app-store

# Timeouts
READINESS_TIMEOUT_MS=120000
HEALTH_RETRY_MS=1500
