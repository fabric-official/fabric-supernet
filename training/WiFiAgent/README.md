# Training — WiFiAgent

Replace `train.py` with your real training pipeline.
Expected output: `agents/WiFiAgent/weights/wifi.bin`
Run `../../tools/update_digest.py ../../agents/WiFiAgent/model.yaml ../../agents/WiFiAgent/weights/wifi.bin` to seal digest.
